(()=>{
const reduce=matchMedia('(prefers-reduced-motion: reduce)').matches;
const io=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting){e.target.classList.add('in');io.unobserve(e.target)}}),{threshold:.12});
document.querySelectorAll('.reveal,.reveal-item').forEach(el=>io.observe(el));
if(!reduce&&innerWidth>809){let target=scrollY,current=scrollY,raf=0;const clamp=v=>Math.max(0,Math.min(v,document.documentElement.scrollHeight-innerHeight));addEventListener('wheel',e=>{if(document.body.classList.contains('menu-open'))return;e.preventDefault();target=clamp(target+e.deltaY);if(!raf)raf=requestAnimationFrame(tick)},{passive:false});function tick(){current+=(target-current)*.09;scrollTo(0,current);if(Math.abs(target-current)<.35){current=target;scrollTo(0,current);raf=0;return}raf=requestAnimationFrame(tick)}addEventListener('scroll',()=>{if(!raf)target=current=scrollY},{passive:true});document.querySelectorAll('a[href^="#"]').forEach(a=>a.addEventListener('click',e=>{const el=document.querySelector(a.getAttribute('href'));if(!el)return;e.preventDefault();target=clamp(el.offsetTop);if(!raf)raf=requestAnimationFrame(tick)}))}
let ticking=false;const parallaxEls=[...document.querySelectorAll('.parallax-img,.service-parallax')];function parallax(){const vh=innerHeight;parallaxEls.forEach(img=>{const p=img.parentElement.getBoundingClientRect(),c=(p.top+p.height/2-vh/2)/vh;const service=img.classList.contains('service-parallax');img.style.transform=`translate3d(0,${-c*(service?34:20)}px,0) scale(${service?1.12:1.04})`});ticking=false}addEventListener('scroll',()=>{if(!ticking){ticking=true;requestAnimationFrame(parallax)}},{passive:true});parallax();
const slides=[...document.querySelectorAll('.testimonial')],dots=[...document.querySelectorAll('.testimonial-dots button')];let idx=0,timer,down=null;function show(i){idx=(i+slides.length)%slides.length;slides.forEach((s,n)=>s.classList.toggle('active',n===idx));dots.forEach((d,n)=>d.classList.toggle('active',n===idx));clearTimeout(timer);timer=setTimeout(()=>show(idx+1),5000)}dots.forEach((d,i)=>d.addEventListener('click',()=>show(i)));const stage=document.getElementById('carousel');stage?.addEventListener('pointerdown',e=>down=e.clientX);stage?.addEventListener('pointerup',e=>{if(down===null)return;const dx=e.clientX-down;if(Math.abs(dx)>45)show(idx+(dx<0?1:-1));down=null});show(0);
const btn=document.querySelector('.menu-btn'),menu=document.querySelector('.mobile-menu');function setMenu(open){menu.classList.toggle('open',open);document.body.classList.toggle('menu-open',open);btn.textContent=open?'CLOSE':'MENU'}btn?.addEventListener('click',()=>setMenu(!menu.classList.contains('open')));menu?.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>setMenu(false)));
})();